#include <stdlib.h>
#include <stdio.h>

int main()
{
    int entier = 0,
    scanf("%d", &entier);
    int test = entier / 2
    double stbl = entier / 2;
    if(test != stbl)
    {
        printf("impair\n");
    }
    else
    {
        printf("pair");
    } 
}